# Aggregate Results

Do not aggregate results prematurely.

This directory should contain cross-experiment summaries only when measurements are sufficiently comparable. Preserve per-experiment raw records under `experiments/`.
